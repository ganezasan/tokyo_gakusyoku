<Alloy>
    <Window class="container">
        <ScrollView id="scrollView">
        	<TableView id="tableView" rowBackgroundColor="white" backgroundColor='transparent'/>
            <Button id="checkinButton" title="Checkin" onClick="checkinSpot" />
        </ScrollView>
    </Window>
</Alloy>
